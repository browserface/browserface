#!/usr/bin/env bash
# Reads Chrome's DevToolsActivePort and prints ready-to-run commands for
# connecting browserface to it.
set -euo pipefail
cd "$(dirname "$0")/.."
[[ -d node_modules ]] || npm install
exec node scripts/find-chrome.mjs "$@"
